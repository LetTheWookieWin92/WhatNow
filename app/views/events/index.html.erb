<div class="section">
    <div class="container">
        <div class="columns is-centred">
            <div class="column is-half">
                <h1 class="title">Current events</h1>
                <% @events.each do |event| %>
                    <div class="content is-medium"><%= event.dateTime %></div>
                    <div class="content is-medium"><%= event.location %></div>
                    <div class="content is-medium">Organised by: <%= event.organiser.firstName %> &nbsp; <%= event.organiser.surname %></div>
                    <% if current_user && event.organiser != current_user && !event.event_registrations.include?(current_user) %>
                        <%= link_to "Join event", create_attendee_path(:user_id => current_user.id, :id => event.id), method: :post, class: "button is-info" %>
                    <% else %>
                        <div>You have registered for the event</div>
                    <% end %>
                    <%= link_to "Event information", event_path(event.id) %>
                    <br/>
                <% end %>
            </div>
        </div>
    </div>
</div>